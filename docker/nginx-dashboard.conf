server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location /app/ {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /app/index.html;
    }

    location /healthz {
        return 200 "OK";
    }
}
